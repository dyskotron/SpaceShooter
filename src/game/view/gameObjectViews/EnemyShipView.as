package game.view.gameObjectViews
{
    import starling.textures.Texture;

    public class EnemyShipView extends GameObjectView
    {

        public function EnemyShipView(texture: Texture)
        {
            super(texture);
        }
    }
}
