package game.view.gameObjectViews
{
    import starling.textures.Texture;

    public class PlayerShipView extends GameObjectView
    {

        public function PlayerShipView(texture: Texture)
        {
            super(texture);
        }
    }
}
